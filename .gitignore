# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.py~
*.py.class
*.py.backup

# Dependencies / environments
.venv/
venv/
env/
.env
.env.local
.env.*
!.env.example

# Data, model, and output artifacts (regenerated by the CLI, not committed)
data/
models/*.pt
models/metadata.json
output/*.xlsx
logs/
# Run manifests and rendered notes (portfolio_agent/provenance). Generated
# per run, one file each, so a week of experiments is a week of untracked
# files — and the provenance they carry is about a run, not about the repo.
runs/

# Logs and temp files
*.log
*.tmp
*.swp
*.swo

# Editors
.vscode/
.idea/

# Coverage reports
.coverage
coverage/
htmlcov/

# Testing
.pytest_cache/
.mypy_cache/

# Build artifacts
*.egg-info/

# Notebook run artifacts. The standalone notebooks download parquet into
# data_cache/ next to wherever they are run, and Jupyter writes checkpoints.
.ipynb_checkpoints/
notebooks/**/data_cache/
data_cache/
